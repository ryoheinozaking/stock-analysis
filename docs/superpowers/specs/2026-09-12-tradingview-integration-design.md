# TradingView 統合 設計

作成日: 2026-09-12

## 背景

TradingView の有料プランを契約しているが、現状は日足チャートを目視で見るだけで活用しきれていない。
Claude と TradingView を接続する MCP サーバーが話題になっており（X の紹介投稿が発端）、
これを機に「トレードにもっと活用する」方法を設計する。

当初は紹介記事に従って `tradesdontlie/tradingview-mcp` を導入する方向で進めたが、
検証の結果いくつか前提が覆ったため、設計をやり直した。本書はその検証結果と方針を記録する。

## 検証で確定した事実

調べ直すコストが高いため、根拠とともに残す。

### TradingView 利用規約は自動収集を明示的に禁止している

利用規約は「スクリプト、API、スクリーンスクレイピング、データマイニング、ロボット等による
自動的なデータ収集を、**その目的を問わず**厳格に禁止する」と定めている。
市場データは「表示のみ」の用途に限ってライセンスされ、表示以外の利用は禁止。
[How bans work](https://www.tradingview.com/support/solutions/43000591357-how-bans-work/) には
この制限に違反した場合アカウントを BAN すると明記されている。

MCP サーバーが行っているのは、まさにこの禁止対象に該当する。
CDP 経由でのデータ取得に加え、ログイン Cookie を使って内部 API
（`pricealerts.tradingview.com`、`pine-facade.tradingview.com`、`symbols_list`）を直接呼んでいる。

一方で、実際に MCP 利用で BAN された報告は確認できなかった。
スター 6,000 規模のツールが約半年運用されて大量 BAN 事件が起きていない状態。
ただしツール作者自身が「アカウント BAN について責任を負わない」と免責している。

**方針**: 利用者のアカウントに関する自己責任の判断として、フル活用する。
ただし失うものを最小化するため、レイアウト設定とウォッチリストの事前退避を A-0 に含める
（Pine ソースを対象外とした理由は A-0 の節に記す）。

### Advanced Charts は個人利用では使えない

TradingView 公式の無料チャートライブラリ [Advanced Charts](https://www.tradingview.com/advanced-charts/) は、
当初「自前サーバーにホストして J-Quants データを流せば規約違反ゼロで使える」と判断したが、これは誤りだった。

ライセンス条件は「**法人**が**公開**のウェブプロジェクトやアプリで使う場合にのみ提供」であり、
「**個人利用・趣味・学習・テスト目的では提供しない**」と明記されている。
stock_analysis は個人の投資判断用ツールのため条件を満たさない。

**代替**: [Lightweight Charts](https://github.com/tradingview/lightweight-charts) は Apache 2.0 の
オープンソースで、個人利用・商用ともに制限がない。Streamlit 用ラッパーも存在し、
特に `streamlit-lightweight-charts-v5` はマルチペイン（価格＋MACD＋RSI の縦積み）に対応する。
系統 B はこちらで実現する。

ただし Apache 2.0 に加えて **帰属表示が必須**。利用者が見えるページに NOTICE ファイル由来の
帰属表示と `https://www.tradingview.com/` へのリンクを掲載する必要がある。
`attributionLogo` チャートオプションでチャート内に表示するのが推奨手段。
これは B-1 の実装要件に含める。

### 日本株は約 20 分遅延、ただし本件では問題にならない

リアルタイムデータ購読なしでは東証銘柄は約 20 分遅延する（リアルタイムは月額約 3 ドルの別購読）。
現状は未契約と見られる。

ただし stock_analysis の戦略はバリュー株 250 日保有・成長株 60 日保有が前提であり、
リアルタイム性は元々必要ない。**逆に言えば、MCP でリアルタイム監視する価値も低い**。
MCP の価値は日足ベースの「チャート判断の壁打ち」と「候補の一括確認」に集中する。

売買タイミングの通知は、日次で足りるため stock_analysis 側で実装する方が筋が通る（将来課題）。

### MCP README が定める禁止事項

README の [Disclaimer 節](https://github.com/tradesdontlie/tradingview-mcp#disclaimer) は、
このツールを以下に使ってはならないと明記している
（行番号は upstream の更新でずれるため、節名と URL で参照する）。

- TradingView の市場データの再配布・転売・商業利用
- TradingView のアクセス制御や購読制限の回避
- **抽出データを用いた自動売買、またはアルゴリズムによる意思決定**
- **Pine Script インジケーター作者の知的財産権の侵害**

後者 2 つは本設計に直接影響する。

1 つ目は A-1（深層分析レポートへの統合）の位置づけに関わる。「完全自動売買」なら明確に該当し、
「人間が最終判断するレポートの材料」ならグレー。A-1 は LLM が投資判断を出力する設計のため、
**限りなくグレーに近い**。利用者の判断としてA-1まで進めるが、この位置づけは記録しておく。

2 つ目はバックアップ設計に影響した。詳細は A-0 の節に記す。

### リポジトリのスナップショット（2026-09-12 時点・変動値）

以下は時点情報であり、確定した事実ではない。参照時は再取得すること。

| 項目 | 値 |
|---|---|
| スター | 6,117 |
| フォーク | 2,650（スター比 43%。転載フォークが多数存在） |
| 未解決 Issue | 241 |
| 最終 push | 2026-07-28（約 1.5 ヶ月停止） |
| ライセンス | **MIT**（`LICENSE` ファイルで確認） |
| 初コミット | 2026-03-16（リポジトリ公開は 2026-03-29） |

GitHub API の `license` フィールドは `Other` を返すが、`LICENSE` ファイルの実体は MIT。
API の要約値ではなくファイルを読むこと。

コードの静的検査結果は問題なし。

- 外部通信先は `*.tradingview.com` とローカル CDP のみ。第三者サーバーへの送信なし
- `eval` / `new Function` / `vm.runIn` はゼロ件
- `child_process` は git 操作・TradingView 起動・自己更新用の `npm ci` のみ
- 環境変数アクセスは CDP 設定と TradingView のパス解決のみ。認証情報の読み取りなし

注意点として `src/core/update.js` に自己更新機構（`git pull` + `npm ci`）がある。
将来のコードが自動で入る経路になるため、リポジトリが乗っ取られた場合の影響範囲が広い。

### 環境: TradingView は MSIX パッケージとして導入済み

Windows 版 TradingView は MSIX 形式（`TradingView.Desktop_n534cwy3pjxzj`）でインストールされている。
`C:\Program Files\WindowsApps\` 配下は権限制限があり、CDP のデバッグポート起動が
"Access is denied" で失敗する既知の問題がある。

リポジトリ側は `tv_launch` ツールで MSIX を自動検出し、失敗時は
`%LOCALAPPDATA%\tradingview-mcp\` へローカルコピー（約 330MB）してそこから起動するフォールバックを実装済み。
ただし環境によっては追加対応が必要との報告もあり、**動作保証はない**。これが A-0 で潰すべき最大の不確実性。

### MCP 設定の書き込み先

`~/.claude/.mcp.json` に書いても読み込まれなかった。
実際に効いているのは `~/.claude.json` のトップレベル `mcpServers` フィールド。
`claude mcp add -s user` で登録するのが正しい。

なお EDINET DB は `claude mcp list` に現れず、デスクトップアプリ独自のコネクタ管理で
登録されているため別系統である。

## スコープ分解

要望は 5 つ挙がったが、技術的に独立した 2 系統に分かれる。

### 系統 A: MCP 活用（規約リスクあり・Claude Code ワークフロー）

- **A-0**: 技術検証＋設定退避
- **A-1**: 深層分析への統合
- **A-2**: 候補絞り込み（Top-N 一括巡回）
- **A-3**: エントリー／保有監視の壁打ち

A-0 は A-1 以降の前提。ここが動かなければ系統 A は成立しない。

### 系統 B: Streamlit チャート刷新（規約クリーン・stock_analysis のコード）

- **B-1**: Lightweight Charts で `components/chart.py`（333 行・Plotly）を刷新

系統 A とは依存関係がなく、独立して進められる。
なお Plotly は `4_portfolio.py` / `7_pipeline_report.py` / `8_backtest_value.py` でも使われているが、
そちらは対象外とする。

**「描画層のみ差し替え」という当初の見積もりは誤り**。`chart.py` を読むと以下が密結合している。

- 一目均衡表の雲は `fill="tonexty"` で `span1.where(bull)` と `span2.where(bull)` を交互に描いて
  間を塗り分ける Plotly 固有の実装。Lightweight Charts に同等機能はなく自前実装が要る
- 価格帯別出来高は `make_subplots` の 2 列目に横向きヒストグラムとして描いており、
  Lightweight Charts の標準機能にない
- 出来高は `secondary_y` でローソク足に重ねている
- `_detect_signals` によるゴールデンクロス／デッドクロス／MACD クロス／RSI 反転の検出結果を
  チャート上に注記として描いている

したがって B-1 は以下の順で進める。

1. **計算層と描画層を分離する** — 指標計算とチャート用データ整形を、描画ライブラリに依存しない形に切り出す
2. **同値テストを作る** — 同じ入力に対し既存 Plotly 版と新実装が同じ値を返すことを検証する
3. 描画アダプタを実装する

さらに前提として、長期データで `AdjC` を直接使うと株式分割でスケールが壊れるため、
`services/split_adjust.normalize_close` を基準に統一しておく必要がある
（プロジェクトの `CLAUDE.md` に記載の既知の罠）。

### 着手順

A-0 → A-1 → （B-1 / A-2 / A-3 は後日判断）

## A-0 設計: 技術検証＋設定退避

### 目的

1. MSIX 環境で MCP が実際に動くか
2. 実チャートから何が取れるか（Supertrend、SQZMOM 等のインジケーター値が読めるか）
3. 同梱 `tv` CLI の能力範囲
4. レイアウト設定とウォッチリストの退避

3 が **A-1 の設計を左右する**。MCP ツールは Claude Code 経由でしか呼べないが、
CLI なら Python から `subprocess` で呼べるため、既存の `scripts/deep_analysis_helper.py` と
同じパターンに乗せられる可能性がある。

### バックアップ対象（Pine ソースは取得しない）

当初は保存済み Pine を全て吸い出して git 管理する設計だったが、以下の理由で取りやめた。

- 利用者が保有する Pine は組み込みまたは第三者の公開スクリプト（`SQZMOM_LB` は LazyBear 作）であり、
  自作のものはない。よって退避する必要がない
- `stock-analysis` リポジトリは **public**（Streamlit Cloud 無料枠の要件）。
  第三者製 Pine を置けば公開の場での再配布に当たり、README の禁止事項
  「Pine Script 作者の知的財産権の侵害」に該当する

退避対象はレイアウト設定とウォッチリストのみ。いずれも利用者自身のデータで権利上の問題はないが、
保有・監視銘柄が推測できるため git には上げない。
既存の `data/` 配下は `.gitignore` で全て除外済み（`data/watchlist.json` も管理外）であり、
同じ方針に揃える。

### サプライチェーン対策

`src/core/update.js` の自己更新機構（`git pull` + `npm ci`）は使わない。

- 検証済みコミット SHA を固定する（本設計時点の HEAD: `c05b8f5755ed8e64ea242de88ddbf46aa24d56a4`）
- 更新が必要な場合は差分をレビューしてから手動で実施
- `tv_update` ツールは呼ばない

**SHA 固定だけでは再現できない**点に注意。`npm audit fix` を適用したため
`package-lock.json` は upstream と差分がある（`src/` は無変更）。
検証環境を特定するには以下も併記する。

```
package-lock.json sha256: 92c8603fa00c1556d6f806cb9a06329df9d10985559da9057af40ed01d09cf8d
```

再現時は SHA を checkout したうえで `npm audit fix` を適用し、
上記ハッシュと一致することを確認する。

### CDP の運用ルール

- 起動後に `127.0.0.1` バインドであることを確認する（外部公開されていないこと）
- 使い終わったらポート 9222 を閉じる。常時開けたままにしない
- 作業単位で起動・終了する

### 手順

1. `claude mcp add -s user tradingview -- node C:/Users/ryohei/tradingview-mcp/src/server.js`
2. Claude Code 再起動
3. `tv_health_check` で接続確認
4. 失敗時は `tv_launch` で MSIX フォールバックの動作を確認
5. `chart_get_state` / `data_get_study_values` で 5016 の実データ取得
6. ウォッチリスト取得・レイアウト情報取得
7. `tv` CLI の能力確認（非対話での JSON 出力が可能か）
8. CDP を閉じ、TradingView が通常起動に戻ることを確認

### 成功条件・撤退条件

「5016 で指標値が一度取れた」だけでは不足。以下を判定表で確認する。

| # | 判定項目 | 成功基準 |
|---|---|---|
| 1 | MSIX 環境での起動 | `tv_health_check` が `cdp_connected: true` を返す |
| 2 | CDP バインド | `127.0.0.1` のみ。外部インターフェースで待ち受けていない |
| 3 | 指標値の取得 | Supertrend / SQZMOM の現在値が数値として読める |
| 4 | Pine 描画の取得 | `data_get_pine_lines` / `labels` が意味のある値を返す |
| 5 | CLI の非対話出力 | `tv` コマンドがパイプ可能な JSON を返す |
| 6 | 起動済み TradingView の復旧 | `taskkill` 後、ログイン状態・レイアウトが保持される |
| 7 | CDP 終了後の復旧 | ポートを閉じた後、通常起動で問題なく使える |
| 8 | 再現性 | 固定 SHA `c05b8f5` で 1〜7 が再現する |

**撤退条件**: 1 または 6 が満たせない場合、系統 A を見送り B-1 へ切り替える。
6 が満たせないということは、検証のたびにチャート環境が壊れることを意味し、
継続利用に耐えない。

3 と 4 が満たせない場合、A-1 の価値（利用者固有のチャート環境を読む）が消えるため、
A-1 は中止し A-0 で得た知見のみ記録する。

> **後日注記**: 実際には判定項目 4 は NG だったが、その後 A-1 が「読み取り」から
> 「書き込み」主体へ転換したため、この撤退条件は適用されなかった。
> 下記「A-1 設計（2026-09-12 確定）」を参照。

### 成果物

| 出力 | 保存先 | git |
|---|---|---|
| チャート状態 JSON | `data/tradingview/state/` | 除外 |
| ウォッチリスト・レイアウト退避 | `data/tradingview/backup/` | 除外 |
| スクリーンショット | `data/tradingview/screenshots/` | 除外 |
| 検証結果（判定表の結果） | 本設計書の「A-0 実施結果」節に追記 | 管理する |

`.gitignore` に `data/tradingview/` を追記する
（`data/*.json` はサブディレクトリ内のファイルにマッチしないため、明示が必要）。

検証結果を会話内の報告だけで終わらせると、次のセッションで A-1 を設計する際に
何が取れて何が取れなかったかが失われる。本設計書に追記して残す。

### リスク

- `tv_launch` は TradingView を `taskkill` で強制終了してから起動し直す。開いている画面が一度閉じる
- MSIX フォールバックが走ると約 330MB のローカルコピーが作られる
- CDP ポート 9222 が開いている間、ローカルの任意プロセスから TradingView を操作可能になる
- TradingView のアップデートで内部 API が変わり、動作しなくなる可能性がある（README も警告）

## A-0 実施結果（2026-09-12）

### 判定表

| # | 判定項目 | 結果 | 備考 |
|---|---|---|---|
| 1 | MSIX 環境での起動 | **OK** | フォールバック不要。`WindowsApps` から直接起動できた。330MB のローカルコピーは作られていない |
| 2 | CDP バインド | **OK** | `127.0.0.1:9222` のみで待ち受け。外部公開なし |
| 3 | 指標値の取得 | **OK** | 下記参照 |
| 4 | Pine 描画の取得 | **NG** | 保有インジケーターが `line.new()` 等を使っていないため全て空。代替あり（下記） |
| 5 | CLI の非対話出力 | **OK** | デフォルトで JSON を stdout に返す。`--json` 不要。パイプでパース可能 |
| 6 | 起動済み TradingView の復旧 | **OK** | `taskkill` 後もログイン状態・レイアウト・描画・ウォッチリストが保持された |
| 7 | CDP 終了後の復旧 | **OK** | ポート閉鎖を確認。通常起動で問題なし（利用者確認済み） |
| 8 | 再現性 | **OK** | SHA `c05b8f5` 固定。ただし `package-lock.json` は `npm audit fix` 適用済み（`src/` は無変更） |

**撤退条件には該当せず。A-1 へ進める。**

### 取得できたデータ

`data_get_study_values` で以下が数値として読めた（TSE_DLY:5016 / 日足）。

| インジケーター | 取得値 |
|---|---|
| Supertrend | Up Trend 3,299.49 / Supertrend 3,668.75 |
| MA/Ichimoku/SAR/BB | 移動平均 3,765 / 3,767 / 3,910 / 3,481、転換線 3,724、基準線 3,910、遅行 3,695、先行1 3,817、先行2 3,868、SAR 3,512、±3σ 4,117 / 3,418 |
| RSI | 47.24 / RSI-based MA 48.37 |
| MACD | Histogram 10.1 / MACD −26.8 / Signal −37.0 |

日本語ラベルのカスタム複合インジケーター（一目均衡表の全要素を含む）がそのまま読めた点が収穫。

`draw_list` → `draw_get_properties` で手描きの描画も価格付きで取れた。

- ray `53kbld`: 1,664 円（2025-12-18）→ 3,312 円（2026-03-31）、右に延長
- この 3,312 は `alert_list` で取れた設定済みアラートの価格と一致していた

`alert_list` と `layout_list` は `internal_api` 経由で正常動作。

### J-Quants との突き合わせ

TradingView の 2026-09-09 のバーの終値 3,909 が、J-Quants の同日終値 3,909.0 と**完全一致**。
分割調整のスケールずれもない。

一方で **J-Quants の `prices.parquet` は 2026-09-09 で止まっており、TradingView の方が
2 営業日新しかった**（2026-09-11 まで保持）。parquet は手動更新のため、
鮮度では TradingView が勝つ場面がある。

### 取得できなかったもの・制約

1. **Pine 描画系 4 ツールは全て空**（`study_count: 0`）。
   画面の Buy / Sell ラベルは Supertrend の `plotshape()` 由来で、`label.new()` ではないため。
   ツールの不具合ではなく、利用者のインジケーター構成がこの API を使っていないことによる

2. **`data_get_study_values` の `inputs` に巨大な暗号化ブロブが混入する**。
   1 回の呼び出しで大量のトークンを消費した。A-1 では `values` のみを抽出する前処理が必須

3. **SQZMOM の値が一部欠落**。画面表示は `−91.0` と `0.0` の 2 値だが、ツールは `0.0` のみ返す

4. **`watchlist_get` が動作しない**（`Watchlist button not found`）。MCP・CLI とも同じ。
   パネルは開いている（`was_open: true`）のに失敗するため、TradingView 3.4.1 または
   日本語 UI に実装が追随していないと判断。`alert_list` / `layout_list` が `internal_api` 経由で
   成功するのに対し、`watchlist_get` だけが DOM 探索実装であることが原因と見られる。
   退避は手動で行う（ウォッチリストを開く → アドバンスドビュー → リストを TXT でダウンロード）。
   手動エクスポートは成功し、「00_ポジション」8 銘柄を `data/tradingview/backup/` に退避済み。
   なお **A-2 でウォッチリストへの書き込み（`watchlist_add_bulk`）を使う構想があるが、
   読み取りが DOM 依存で失敗している以上、書き込みも同じ実装なら動かない可能性が高い**。
   A-2 着手時に最初に検証すべき項目

5. **`tv_health_check` の更新チェックが誤った SHA を返す**。
   `src/core/health.js:17` の `execSync('git rev-parse HEAD')` が cwd を指定していないため、
   MCP サーバーとして起動されると Claude Code の作業ディレクトリ（この場合 stock_analysis）の
   リポジトリを見てしまう。CLI から実行すれば正しい値が返る。
   `tv_update` を使わない方針のため実害はないが、`update_available` の表示は信用しないこと

6. **フィードは `TSE_DLY`**（20 分遅延）であることが確定。日足利用では影響しない

### A-1 への申し送り

- **実装形態は案 2（CLI 経由のヘルパースクリプト）が選択可能**。CLI は全機能をカバーし、
  デフォルトで JSON を stdout に返すため、`scripts/deep_analysis_helper.py` と同じ形で
  Python から `subprocess` 呼び出しできる
- 取得すべきは `values` のみ。`inputs` は捨てる
- Pine 描画系は使わない。代わりに `data_get_study_values` ＋ `draw_list` / `draw_get_properties` ＋ `alert_list`
- J-Quants parquet が古い場合、TradingView の方が新しい終値を持つ。
  深層分析で両者を併記する際は as_of の差を明示すること

> **後日注記**: この申し送りは A-0 完了時点のもので、A-1 を「読み取り主体」と想定していた。
> その後の設計転換により実装形態は**案 1（SKILL.md に手順を追加）**へ変更された。
> CLI 経由を採用するのは読み取り部分のみ。確定内容は下記を参照。

## A-1 設計（2026-09-12 確定）

### 設計の転換: 読み取り主体から書き込み主体へ

当初案は「TradingView から読んでレポートに書く」だったが、利用者の提案により
**「分析結果をチャートに描き込む」** を主軸に変更した。

判断理由: J-Quants で MA・RSI・MACD・52 週レンジ・出来高はすでに取得できており、
読み取りで TradingView が追加するのは Supertrend や一目均衡表といった補助的な値にとどまる。
対して「分析結果をチャートに描く」は **J-Quants では原理的に実現できない**。
分析が文書で終わらずトレード画面に残り、次にチャートを開いたとき自分の判断が線として見える。
当初の要望「トレードに活用したい」に最も直接応える。

読み取りは補助として残すが、主目的ではない。

### 技術検証結果（2026-09-12 実施）

書き込み系は A-0 で未検証だったため、設計確定前に実機で確認した。

| 操作 | 結果 |
|---|---|
| `draw_shape`（horizontal_line、色・ラベル・価格指定） | 動作。画面表示も正常 |
| `draw_get_properties` での設定反映確認 | `linecolor` `text` `price` すべて反映 |
| `alert_create` | 動作。`alert_id` を返す |
| `draw_remove_one`（ID 指定） | 動作。既存描画は無傷 |
| `alert_delete`（ID 指定） | 動作。既存アラートは無傷 |
| 元の状態への復元 | 描画 3 件・アラート 1 件が検証前と完全一致 |

重要な確認事項:

- アラートは `TSE:5016`（リアルタイムシンボル）で登録される。チャートが `TSE_DLY`（遅延）でも
  アラート自体は実価格で発火する
- 引いた線は TradingView のクラウドに保存され、CDP を閉じた後も残る
- **`draw_clear` と `alert_delete(delete_all)` は使用禁止**。利用者の手描き描画・既存アラートを
  巻き込んで消すため。必ず ID 指定で個別に削除する

### 実行トリガー

株価水準・エントリータイミング・利確損切りが論点になったときのみ実行する。
事業構造や財務が中心の分析ではスキップする。

### CDP 起動の運用

**分析時にその都度再起動する**。

デバッグポートはプロセス起動時の引数でのみ開く（Chromium/Electron の仕様）。
起動済みプロセスに後から開く手段はなく、`kill_existing: false` を指定しても
TradingView はシングルインスタンスのため新プロセスが即終了し CDP は開かない。

再起動のコストは低い。A-0 と A-1 の検証で計 4 回再起動したが、毎回数秒で
ログイン状態・レイアウト・インジケーター・手描き描画・ウォッチリストがすべて復旧した。

ただし**再起動する旨を伝えてから実行する**。確認待ちはしないが、無言で画面を閉じない。

### 処理フロー

```
1. tv_health_check で接続確認
   - 未接続かつ TradingView 未起動 → tv_launch
   - 未接続かつ起動中 → 再起動する旨を伝えて tv_launch

2. チャート文脈を記録（復元用）
   chart_get_state → symbol / resolution / studies
   layout_list     → 現在の layout_id
   ※ 銘柄だけでなく時間足・レイアウトまで控える

3. chart_set_symbol で分析対象へ切り替え

4. 【読み取り・補助】CLI 経由で指標値を取得

5. 分析本体（既存ロジック。Claude が水準を判断）

6. 【書き込み・二段階更新】旧を消してから作るのではなく、作ってから消す
   a. 新しい水準を draw_shape で描画（旧線は残したまま）
   b. エントリーと損切りに alert_create
   c. draw_list / alert_list で全件の作成成功を検証
   d. 新 ID を記録ファイルへ保存
   e. 検証が通って初めて、記録済みの旧 ID を個別削除
   f. 途中で失敗した場合は、作成済みの新規分だけを ID 指定で削除し、
      旧状態をそのまま残す（前回の水準を失わない）

7. 記録した文脈へ復元（銘柄・時間足・レイアウト）
```

**なぜ二段階更新にするか**: 「旧削除 → 新規作成」の順だと、作成の途中で失敗したときに
前回の有効な水準だけが消え、中途半端な状態が残る。分析結果を失うのは
線が一時的に重複することより重い損害である。重複は数秒で解消される。

### 描画規約（2026-09-12 実機調整により確定）

> **2026-09-13 改訂**: 帯の文字ラベルを廃止し、価格は水平線で右の価格軸に出す方式へ変更した。
> 現行の規約は末尾の「描画規約の改訂（2026-09-13）」節を参照。本節は経緯として残す。

**線ではなく帯（rectangle）で描く。** 利用者のチャートには既に 20 本近い線
（Supertrend・MA 6 本・一目均衡表 5 要素・BB・SAR）が乗っており、線を足すと必ず埋もれる。
実線・破線・点線と色（緑/青/赤/オレンジ/マゼンタ）をひと通り試したが、いずれも判別できなかった。
水準の**間**を半透明で塗り分け、境界そのものが水準を示す方式に到達した（フィボナッチの帯と同じ発想）。

| 帯 | 範囲 | 色 | ラベル例 |
|---|---|---|---|
| 損切り〜エントリー | 下段 | `#ff5252`（赤） | `[深層] 損切3,200 - エントリー3,500` |
| エントリー〜利確① | 中段 | `#00e676`（緑） | `[深層] エントリー3,500 - 利確①4,400` |
| 利確①〜利確② | 上段 | `#2979ff`（青） | `[深層] 利確①4,400 - 利確②4,700` |

共通の `overrides`:

```json
{
  "fillBackground": true,
  "transparency": 85,
  "linewidth": 0,
  "fontSize": 12,
  "bold": true,
  "horzLabelsAlign": "right",
  "vertLabelsAlign": "bottom"
}
```

実装上の要点:

- **`textColor` と `fontSize` はキャメルケース**。`horizontal_line` は `textcolor` / `fontsize`
  （小文字）だが矩形では通らず、既定の紫・14pt のままになる
- `linewidth: 0` で枠線を消す（実機で消えることを確認）
- 時間範囲は直近バーを基準に **1 年前 〜 3 ヶ月先**。ズームしても見切れない
- 帯には価格軸ラベルが出ないため、**ラベル文字列に価格を必ず入れる**
- 最上段の帯だけ `vertLabelsAlign` を `"top"` にしてラベルの重なりを避ける
- 接頭辞は **`[深層]` / `[飛躍]`** で分析種別を示す。手描きの描画とも区別できる

アラート:

- エントリーと損切りの 2 水準のみ。利確には作らない（アラート過多を避ける）
- **アラートを作るとその価格に点線が表示される**（TradingView の仕様）。
  帯の境界と重なるが、アラートの存在が視覚的に分かるため許容する

### 深層分析と飛躍分析の共存

同一銘柄を両方で分析した場合、**後から実行した分析が前のゾーンを上書きする**。
削除判定は `layout_id` + `tv_symbol` の一致のみで、`analysis_type` は照合に使わない。

共存させない理由: 帯が 3 本ずつ計 6 本になり、水準が近いと重なって判読できなくなる。
どちらの判断かはラベル接頭辞（`[深層]` / `[飛躍]`）で識別する。

### ID 記録

`data/tradingview/drawings/<TICKER>.json`（git 管理外）に保存する。

**銘柄だけをキーにしてはならない。** TradingView の描画はレイアウトごとに別管理されるため、
銘柄のみで照合すると別レイアウトの `[DA]` 線を削除対象と誤認する余地がある。
チャート文脈（レイアウト・銘柄・時間足）ごとに記録を分ける。

```json
{
  "ticker": "5016",
  "contexts": [
    {
      "layout_id": 65538569,
      "tv_symbol": "TSE_DLY:5016",
      "resolution": "1D",
      "updated_at": "2026-09-12",
      "analysis_type": "deep-analysis-jp",
      "shapes": [
        { "id": "jQbi4o", "role": "stop_loss", "price": 2900, "label": "[DA] 損切り 2,900" }
      ],
      "alerts": [
        { "id": 5593961826, "role": "stop_loss", "price": 2900 }
      ]
    }
  ]
}
```

`data/da_cache/<TICKER>.json` は `deep_analysis_helper.py` が上書きするため使わない。

**削除判定のルール**:

- `layout_id` と `tv_symbol` が**両方一致**する context の ID のみを削除対象とする
- 一致する context がなければ削除は行わず、新しい context として追記する
- ID が無効（利用者が手で消した等）の場合はエラーを無視して続行する

`resolution` は水平線の表示に影響しない（価格のみで定義されるため）が、
どの時間足で判断したかの記録として残す。照合条件には含めない。

### 読み取り（補助）

`data_get_study_values` を MCP ツールで直接呼ぶと `inputs` の暗号化ブロブで
大量のトークンを消費する（A-0 で確認）。CLI とパイプで `values` のみ抽出する。

```bash
cd ~/tradingview-mcp && node src/cli/index.js values | <values 抽出>
```

取得した Supertrend・一目均衡表・SAR・BB の値は、レポートの 4.5 に
「J-Quants 客観指標」と並べて 2 層で記載する。両者が食い違う場合は指摘する。

### エラー方針

**分析そのものは絶対に止めない。** 以下はすべて警告を出して従来どおりの分析を完遂する。

- CDP に接続できない
- 銘柄の切り替えに失敗した
- 描画・アラートの作成に失敗した
- 前回 ID の削除に失敗した

TradingView 連携は付加価値であり、分析の前提条件ではない。

### 事前作業: SKILL.md のバックアップ

`~/.claude/skills/` は **git 管理されておらずバックアップも存在しない**。
`deep-analysis-jp/SKILL.md` は 605 行・42KB で、投資哲学・判定フレームワーク・
レポート構成・銘柄実例が蓄積された資産であり、破損すると復元手段がない。

変更前に `data/tradingview/backup/` へタイムスタンプ付きで退避する。
stock_analysis は public リポジトリのため、投資手法が書かれた SKILL.md を
git 管理下に置いてはならない。

### 変更対象ファイル

| ファイル | 変更内容 | git |
|---|---|---|
| `~/.claude/skills/deep-analysis-jp/SKILL.md` | Step 1.6（TradingView 連携）を追加、4.5 を 2 層構成に | 管理外 |
| `~/.claude/skills/leap-analysis-jp/SKILL.md` | 同上 | 管理外 |
| `data/tradingview/drawings/` | 新規作成（ID 記録置き場） | 除外済み |
| `data/tradingview/backup/` | SKILL.md の退避先 | 除外済み |

### A-1 に残る規約上の論点

**A-1 は規約適合を保証しない実験的・任意のローカル支援である。** これを前提に扱う。

「グレー」という表現は実態より弱い。A-1 が実際に行うのは以下であり、
MCP の [Disclaimer 節](https://github.com/tradesdontlie/tradingview-mcp#disclaimer) が
禁止事項として挙げる「抽出データを用いたアルゴリズムによる意思決定」に接近している。

1. CLI 経由で TradingView から指標値を読み出す（抽出）
2. その値を含む材料から LLM が売買水準を決める（意思決定）
3. 決めた水準でアラートを自動作成する（自動化）

書き込みへ主軸を移しても、プログラムによる操作である点は読み取りと変わらない。
TradingView 利用規約が自動的なデータ収集を目的を問わず禁じていること、
市場データが表示のみの用途に限ってライセンスされていることも変わらない。

一方で、描くのは人間が見るための線であり、発注や自動売買には一切接続しない。
アラートも TradingView の通知機能を使うだけで約定には結びつかない。

利用者の判断として A-1 まで進めるが、以下を前提とする。

- 完全自動の売買判断や発注には接続しない
- 出力はあくまで人間が読むレポートと、人間が見るためのチャート上の線である
- アラートは通知までで、発火をトリガーにした自動処理は組まない
- TradingView 側の仕様変更や規約運用の変化があれば、A-1 を停止して再評価する

**これは技術的な撤退条件とは性質が異なり、運用を続ける限り残り続ける受容リスクである。**
A-0 の判定表のように「満たせなければ撤退」と判定できる類のものではない。

なお外部レビュー（ChatGPT-5）では「着手順を B-1 → A-0 → A-1 に変え、A-1 は規約判断後に再審査すべき」
との提案があった。検討の結果、A-0 の判定表で撤退条件を明確にしたこと、および
A-1 を上記の前提付きとすることで、当初の A-0 → A-1 の順を維持する。

### 実装形態（確定）

当初の案 1／案 2／案 3 からの選択として、**案 1（`SKILL.md` に手順を追加）を採用する**。

理由: 書き込みは Claude が判断した水準をその場でツールに渡す処理であり、
Python スクリプトを経由すると水準を引数で渡し直す手間が増えるだけで利点がない。
ID の記録もファイル書き込み 1 回で済むため、専用スクリプトは不要（YAGNI）。

ただし**読み取りだけは CLI 経由**とする。`data_get_study_values` の暗号化ブロブを
コンテキストに入れないための措置で、案 2 の発想をこの一点に限って採用する。

専用スクリプトを作らないため、A-2（候補の一括巡回）で再利用可能な部品は生まれない。
A-2 着手時に改めて設計する。

## A-1 実施結果（2026-09-12）

### 変更したファイル

- `~/.claude/skills/deep-analysis-jp/SKILL.md` — Step 1.6 を追加（605 → 748 行）、4.5 を 2 層構成に
- `~/.claude/skills/leap-analysis-jp/SKILL.md` — Step 1.6 を追加（327 → 465 行）、「4. タイミングと価格」を拡張
- 退避: `data/tradingview/backup/SKILL_*_2026-09-12.md`（sha256 で同一性を確認済み）

### 検証結果

| 項目 | 結果 |
|---|---|
| 初回描画 | **OK** 水平線 4 本 + アラート 2 件を作成 |
| 二段階更新（作成 → 検証 → 旧削除） | **OK** 一時的に 11 件へ増え、旧削除後に 7 件へ収束 |
| 既存の手描き描画・アラートの保全 | **OK** 全工程で `53kbld` / `DuNpvv` / `r8g3K4` / `4806548286` は無傷 |
| 元の状態への復元 | **OK** 描画 3 件・アラート 1 件で検証前と一致 |

### 見た目の調整で判明したこと

当初の「色付きの水平線」案は**実用に耐えなかった**。利用者のチャートには
Supertrend・MA 6 本・一目均衡表 5 要素・BB・SAR で 20 本近い線が乗っており、
以下をすべて試したが判別できなかった。

1. 実線・太さ 2（緑/青/赤） → 既存線に埋没
2. 破線・太さ 3 → 「チカチカして見にくい」
3. 点線・オレンジ/マゼンタ → 既存の点線（BB・SAR）と混同
4. 右端の余白だけに短い線（`trend_line`） → 「線が切れているのが気持ち悪い」

最終的に、利用者の提案（フィボナッチのような透過帯）を採用して解決した。
**水準の間を半透明で塗り分け、境界そのものが水準を示す**方式。
詳細は上記「描画規約」節。

### 実装上の落とし穴

1. **`capture_screenshot` は使わない**。`wait_for_render: true` でハングし、MCP のタイムアウト
   （30 分）まで復帰しなかった。さらに `region` 指定はビューポートをリサイズするため、
   ハングすると**ウィンドウが縮んだまま復元されず壊れた状態で残る**（実際に発生し、再起動で復旧）。
   視覚確認は利用者に直接画面を見てもらう
2. **矩形とその他でプロパティ名の大小文字が違う**。`horizontal_line` は `textcolor` / `fontsize`、
   `rectangle` は `textColor` / `fontSize`。誤ると既定値（紫・14pt）のままになる
3. **描画は銘柄ごとに保存される**。後片付け中に利用者がチャートを 9984 へ切り替えていたため
   `draw_list` が 1 件しか返さず一瞬データ損失を疑ったが、5016 に戻すと 6 件すべて健在だった。
   ID が見つからないときは**まず銘柄を確認する**
4. アラートを作るとその価格に点線が出る（TradingView の仕様）。帯の境界と重なるが許容する

### 未検証

- ~~本番の分析フローに組み込んだ状態での実行~~ → 2026-09-12 の 6203 豊和工業の深層分析で実施。
  描画・アラート・二段階更新（エントリー 1,700 → 1,200 への描き直し）が動作した。
  見た目の問題は次節の改訂で対応

## 描画規約の改訂（2026-09-13）

6203 豊和工業の深層分析で本番フローに初めて組み込み、利用者が実画面を見て 3 点を指摘した。

### 判明した問題

1. **帯のラベルが隣の帯にはみ出す**。rectangle の `vertLabelsAlign` は、`bottom` で帯の上の縁の
   **外側**、`top` で下の縁の**外側**にラベルを置く（帯の内側ではない）。利確①〜利確②の帯は
   60 円幅と狭く、緑帯（エントリー〜利確①）のラベルが青帯の中に表示されたため、
   「青帯の説明がエントリー〜利確①になっている」と読まれた。
   「最上段だけ `top` にすれば重ならない」という旧規約は、内側に置かれるという誤った前提に立っていた
2. **帯が途中で切れる**。右端を「直近 + 3 ヶ月」で固定していたため、先へスクロールすると帯が終わる
3. **アラートの破線が邪魔**。利用者がアラートごと手動で削除した

### 変更後の規約

- 帯（rectangle）: 文字を入れない。`extendRight: true` で右へ無限に延長する。色・透過率は従来どおり
- 価格の表示: 4 水準に水平線（`horizontal_line`、`showPrice: true`）を置き、**右の価格軸に数字だけを出す**

| 水準 | `linecolor` | 軸の数字の色 | 色の由来（利用者のチャート） |
|---|---|---|---|
| 損切り | `rgba(255, 82, 82, 0.15)` | 赤 | Supertrend 下降ライン `#FF5252` |
| エントリー | `rgba(38, 166, 154, 0.15)` | 緑 | MACD ヒストグラム `#26a69a` |
| 利確① / 利確② | `rgba(41, 98, 255, 0.15)` | 青 | MACD 線 `#2962ff` |

- ID 記録に `lines` 配列を追加した。再分析時は帯・水平線・アラートをそれぞれ ID 指定で削除する

### 色の決定で判明した TradingView の挙動

水平線の軸ラベルは**線の色みを使うが、透け具合（rgba の不透明度）は無視する**。
線とラベルで色みや明るさを別々にする設定は無い。試した順に:

1. 帯と同じ鮮やか色・不透明 → 線も数字も濃く、違和感がある
2. 帯と同じ色・不透明度 0.15 → 線は薄くなったが数字は鮮やかなまま（ここで上記の挙動に気付いた）
3. 背景色 `#131722` に帯の色を 15% 混ぜた不透明な暗色 → 数字の背景まで暗くなり「超見にくい」
4. インジケーター由来の色・不透明 → 数字は見やすいが線が濃い
5. **4 の色・不透明度 0.15 → 採用**。線は帯になじみ、数字は鮮やか

### アラートの扱い（2026-09-13 決定）

アラートの破線を非表示にする設定は、チャートのプロパティと内部 API を探した範囲では見つからなかった。
内部ではアラートの作成・削除に連動して破線が描画・消去される作りになっている。

利用者の判断により、**描き込み時にアラートは作らない**。アラートが必要な場合は利用者が自分で作る。
価格到達の通知は stock_analysis 側の日次実装（保留事項参照）に委ねる。
再分析時の削除は記録済みの ID に限るため、利用者が手で作ったアラートには触れない。

### 変更したファイル

- `~/.claude/skills/deep-analysis-jp/SKILL.md` — Step 1.6.3 を改訂（748 → 773 行）
- `~/.claude/skills/leap-analysis-jp/SKILL.md` — Step 1.6.3 を改訂（465 → 490 行）

## 保留事項

- 売買タイミングの通知は stock_analysis 側で日次実装する方向（本設計のスコープ外）
- B-1 の詳細設計は A-0/A-1 完了後
- リアルタイムデータ購読（月額約 3 ドル）は現状不要と判断
