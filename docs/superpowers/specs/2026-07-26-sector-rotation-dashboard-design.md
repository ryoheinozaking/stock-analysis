# セクター回転検知ダッシュボード — 設計書

- 日付: 2026-07-26
- ステータス: 承認待ち（Codex レビュー反映済み・rev2）
- 元ネタ: [KabuTrend /trend](https://kabutrend.com/trend) を参考 UI とし、指標・閾値・検証は自前で作る

---

## 1. 目的と背景

KabuTrend は日本株を約275の手作りテーマにグループ化し、テーマ単位で「資金の回転」を
日次観測するダッシュボード（月2,980円）。裏の事前計算済み JSON を解析した結果、その
計算ロジック自体は本プロジェクトの `data/prices.parquet`（全銘柄日次 OHLCV + 売買代金 Va）
と `data/stock_cache.parquet`（計算済みモメンタム・SEPA 指標）で再現可能と判明した。

KabuTrend の唯一の堀は「細かいテーマ・マスタ」だが、本ダッシュボードは **J-Quants の
S33 業種分類（34グループ）を軸**に開始する。回転検知の計算式は軸に依存しないため、
将来テーマ表を差し替えれば細かい軸へ移行できる設計にしておく。

### 設計スタンス（重要）

KabuTrend は **参考 UI・機能セットのインスピレーション源**として扱う。**指標の定義・
閾値・有効性の検証はすべて自前データで独自に決める**（分位点・バックテスト）。KabuTrend の
内部 JSON から観測したパラメータを実装仕様に写経しない。これはプロダクトの独自性の
観点でも、法務・各社利用規約の観点でも安全側の方針。「中身が読めたから再現する」のでは
なく「参考にしつつ独自に作る」。

### スコープの線引き（調査で確定）

| 段階 | 内容 | データ源 | 今回 |
|---|---|---|---|
| 段階1 | セクター回転・ランキング・銘柄需給シグナル | prices.parquet + stock_cache | **含む** |
| 段階2 | 信用倍率・信用残増減 | JPX 公式「銘柄別信用取引週末残高」(無料DL) | **含む** |
| 段階3 | リアルタイム板・VWAP・分足 | kabuステーションAPI（50銘柄上限・要口座） | **対象外**（別プロジェクト） |

- 段階3 は kabuステーションAPI が **同時50銘柄上限**のため市場横断スキャンに使えず、
  KabuTrend 型の市場横断ダッシュボードとは用途が異なる。将来「保有・監視50銘柄の
  リアルタイム版」として別途検討する。
- ユーザの J-Quants プランは Free/Light（信用データ非開放）のため、段階2 の信用残は
  J-Quants ではなく **JPX 公式の無料ダウンロード**（口座不要）から取得する。

---

## 2. アーキテクチャ

計算ロジックを純粋関数として `services/rotation_service.py` に隔離し、Streamlit ページ
（`pages/9_sector_rotation.py`）は薄い UI 層 + `@st.cache_data` ラッパーに徹する。
KabuTrend が事前計算 JSON で提供する内容を、本アプリは「純粋関数 + セッションキャッシュ」
で置き換える。都度計算でも実測 1.5 秒（初回ロード込み、90営業日 × 34業種）で実用範囲。

```
pages/9_sector_rotation.py   … UI 層（Streamlit + Plotly、@st.cache_data）
        │  呼び出し
        ▼
services/rotation_service.py … 純粋計算（隔離・テスト可能）
        │  直読み
        ▼
data/prices.parquet          … 全銘柄日次 OHLCV + Va（既存・Streamlitがメンテ）
data/stock_cache.parquet     … 銘柄別モメンタム/SEPA/sector（既存）
data/margin.parquet          … 段階2: JPX信用残（新規・下記パイプラインで生成）
```

### モジュール境界

- `rotation_service.py` は Streamlit に依存しない純粋関数のみ（`import streamlit` しない）。
  入力は DataFrame、出力は DataFrame/dict。これにより単体テストとバッチ化の両方が可能。
- ページ側が `@st.cache_data` でラップし、キャッシュ制御と UI 描画のみ担当。

---

## 3. データモデル

### 3.1 業種軸

`stock_cache.parquet` の `sector` 列（S33 業種名、34グループ）を銘柄→業種の対応表とする。
構成銘柄が 3 未満の業種は集計から除外（統計的に無意味なため）。`prices.parquet` の
`Code`（5桁）を `stock_cache.code` で業種に写像。写像できないコード（ETF・上場廃止等）は
`unknown` として除外。

### 3.2 日次セクター系列（prices.parquet 直読み、直近90営業日）

- **セクターリターン** = 構成銘柄の日次リターンの等加重平均。
  分割スケール破綻を避けるため `services/split_adjust.normalize_close` で末尾日スケールに
  正規化した close 系列から pct_change を取る（CLAUDE.md の分割対応ルール遵守）。
- **セクター売買代金** = 構成銘柄 `Va` の合計。

> **90日窓と52週シグナルの関係（Codexレビュー反映）**: セクター回転の集計は直近90営業日で
> 足りるが、52週新高値/安値は260営業日相当を要する。これを prices.parquet から都度計算
> するのではなく、**`stock_cache` の既存列 `sepa_from_low`（52週安値からの%）・
> `sepa_from_high`（52週高値からの%）・`mom_new_high`（新高値フラグ）を再利用**する。
> よって rotation_service が prices.parquet から読むのは90日窓のみで一貫する。

### 3.3 集計の加重方針（Codexレビュー反映）

大型業種・構成銘柄数の多い業種にランキングが歪まないよう、加重方針を明記する:

- **セクターリターン・breadth**: 等加重（構成銘柄の単純平均・単純比率）
- **turnoverRatio / 売買代金ペース**: いずれも「その業種自身の過去比」（当日 ÷ 自業種の
  過去中央値/平均）で算出するため、業種間の絶対規模に依存しない
- 資金流入スコアの5成分はすべて **業種内相対値**（過去比・シェア・比率）に正規化してから
  合成する。したがって業種間ランキングは構成銘柄数・時価総額規模に対して中立

---

## 4. 収録機能

### A. 市況サマリー（画面上部）

- **テーマ温度ゲージ**（0-100）: 上昇業種比率と中央 breadth を合成
- KPI 行: 上昇業種比率(%)・出来高急増業種数・鮮度カウント（上昇中/勝ち続け/失速）
- **データ基準日**を明示。`prices.parquet` の最終日が古ければ Streamlit「データ更新」を促す

### B. セクター回転（4指標）

| 指標 | 計算式 | KabuTrend 対応 |
|---|---|---|
| 出来高急増 | 当日セクター売買代金 ÷ 直近20日中央値 = turnoverRatio。降順ランク | `turnoverRatio` |
| 鮮度 | 週リターン(5日)ランク・月リターン(20日)ランクを算出。`rankDelta = weekRank − monthRank`（負=最近急浮上）。**上昇中**(週良・月悪) / **勝ち続け**(両方良) / **失速**(週悪・月良) の3分類 | `freshness` |
| 資金流入 | 5成分の合成スコア（下記 4.B.1） | `fundFlow` |
| テーマ温度 | 上昇業種比率 + 中央 breadth を 0-100 に合成 | `themeTemperature` |

閾値（鮮度の分類境界など）は `rotation_service.py` 冒頭に定数として外出しし、後から
調整・検証できるようにする。**初期値は自前データの分位点から決める**（例: 週/月スコアの
上位・下位の分位点を過去データで算出）。KabuTrend の内部パラメータは写経せず、最終的な
閾値はバックテスト（既存 `backtest_value_service` 等の枠組みを流用）で検証して確定する。

#### 4.B.1 資金流入スコア（5成分の合成）

KabuTrend の fundFlow 5成分を、板情報のない日次データで近似する:

1. **売買代金ペース** (turnoverPace): 当日売買代金 ÷ 直近平均、0-100 正規化
2. **持続日数** (persistence): 平均を連続で上回った日数
3. **直近リターン** (flowReturn): 直近リターンのスコア化
4. **上昇銘柄比率** (breadth): 構成銘柄のうち上昇した割合
5. **上昇売買代金シェア** (`up_turnover_share`): 上昇銘柄の売買代金 ÷ 業種売買代金

> **命名と割り切り（Codexレビュー反映）**: KabuTrend の flowDominance は板/約定方向を
> 使うと推測されるが、本アプリに板情報がないため「上昇銘柄の売買代金シェア」で代替する。
> これは「買い主導」を意味しないため、内部名も UI 表示も **`上昇売買代金シェア`
> (`up_turnover_share`)** とし、`flowDominance` という強い語は使わない。

### C. ランキング（タブ切替、KabuTrend の /rankings 相当）

- タブ: 値上がり / 値下がり / 出来高急増 / 資金フロー / モメンタム
- **各銘柄にテーマ内順位（selfRank / 業種内N銘柄中X位）を付与** — 「使いやすいランク付け」の核

### D. 銘柄レベル需給シグナル（段階1の範囲）

- 5MA/25MA/75日線乖離率、52週新高値/安値、ゴールデン/デッドクロス(5/25・25/75)、
  出来高前日比・出来高倍率
- 大半は `stock_cache` の `mom_*` / `sepa_*` を再利用。不足分のみ prices.parquet から算出

### E. ドリルダウン

- 業種を選択 → 構成銘柄を需給シグナル + テーマ内順位付きで一覧 → 銘柄詳細へ遷移。
  KabuTrend の「テーマボーナス」（個別モメンタム + 所属業種が強ければ加点）を再現。

> **遷移方式（Codexレビュー反映・コード検証済み）**: `pages/2_stock_detail.py` は
> `st.session_state["selected_code"]`（5桁コード）を読む実装で、URL query param は
> 未対応（実コードで確認）。したがってランキング表からの遷移は **各行にボタンを置き、
> 押下で `st.session_state["selected_code"] = <5桁code>` をセットしてから
> `st.switch_page("pages/2_stock_detail.py")` を呼ぶ**方式にする。詳細ページ側の改修は不要。

---

## 5. 段階2: JPX 信用残パイプライン（新規）

### 5.1 データ源

JPX 公式「[銘柄別信用取引週末残高](https://www.jpx.co.jp/markets/statistics-equities/margin/05.html)」
（無料・口座不要）。J-Quants が再販している元データそのもの。

**実ファイルのスパイク結果（2026-07-26 実施・Codexレビュー反映）**:

- **形式は Excel ではなく PDF**（当初想定を訂正）。1ファイル約836KB、全2,675銘柄（貸借銘柄）
- URL 規則: `.../margin/tvdivq0000001rnl-att/syumatsu{YYYYMMDD}00.pdf`
  （例: `syumatsu2026071700.pdf` = 2026-07-17 申込み現在）
- 公表: 週次、火曜16:30目安（申込日=前週金曜）
- **保持は直近5週分のみ**ページに掲載 → 履歴を貯めるには**毎週アーカイブ保存が必須**
- 各銘柄の収録項目: コード（**5桁、prices の Code と一致**）・銘柄名・ISIN・
  **売残高・買残高・各前週比**・一般信用/制度信用の内訳
- `信用倍率 = 買残高 ÷ 売残高`（自前計算）。`信用残増減`は**前週比が原ファイルに入っている
  ため履歴なしで算出可能**
- **取得可否**: `curl --ssl-no-revoke`（ブラウザ UA）で Claude Code シェルから HTTP 200 で
  取得成功。J-Quants と異なり **JPX は自動DL可能**。ただし恒久運用は Streamlit の
  「データ更新」に組み込む（prices と同じ導線に揃える）

> **PDF パース方針**: 本プロジェクトの PDF ルールに従い `scripts/extract_pdf.py`（PyMuPDF）で
> テキスト抽出 → 正規パターン（5桁コード + ISIN + 数値列）で 2,675 行をパースする専用
> パーサを書く。列レイアウトが規則的なのでパース可能と実物で確認済み。
> **未確定リスク**: 2025末〜2026にかけて JPX が公表形式を変更する告知あり。パーサは
> ヘッダ行検出とフォーマット版数を吸収できる作りにし、パース失敗時はエラーで気付ける
> ようにする（黙って壊れない）。

### 5.2 パイプライン

```
JPX週次PDF(syumatsu*.pdf)  →  extract_pdf.py(PyMuPDF)  →  専用パーサ
   → data/margin/{YYYYMMDD}.parquet として週次アーカイブ保存(履歴構築)
   → rotation_service が最新を読み込み → 信用倍率(買残/売残)・信用残増減(前週比) を算出
```

### 5.3 拡張フック

`rotation_service.py` に信用データ読み込みの関数境界を最初から用意する。margin.parquet が
存在すれば信用指標を有効化し、無ければ段階1 の指標のみ表示（グレースフルデグレード）。
これにより、JPX パイプラインの実装完了前でもダッシュボード本体は動作する。

---

## 6. UI レイアウト（Streamlit + Plotly）

装飾絵文字は使わない（機能系 ✅⚠️ のみ）。既存 `components/chart.py` が Plotly を使うため
本ページも Plotly で統一。

1. 上部: テーマ温度ゲージ + KPI 行 + データ基準日
2. 出来高急増セクション（横棒 + テーブル）
3. 鮮度セクション（上昇中 / 勝ち続け / 失速 の3カラム、順位移動を可視化）
4. 資金流入セクション（合成スコア順テーブル + 5成分の内訳）
5. 信用需給セクション（段階2。信用倍率・信用残増減ランキング。margin.parquet があれば表示）
6. ランキングセクション（タブ切替）
7. ドリルダウン: 業種選択 → 構成銘柄モメンタム/需給表 → 詳細ページへ

---

## 7. ファイル構成

- `services/rotation_service.py` — 純粋計算（隔離・テスト可能・信用フック内蔵）
- `pages/9_sector_rotation.py` — UI + `@st.cache_data`
- `tests/test_rotation_service.py` — 合成データによる単体テスト
- （段階2）JPX 信用残 PDF のダウンロード + PyMuPDF パースは `services/` 内に追加
  （`margin_service.py` 等・実装時にモジュール名確定）。`scripts/extract_pdf.py` を利用
- （段階2）`data/margin/{YYYYMMDD}.parquet` — 週次アーカイブ（履歴構築のため週ごとに保存）

---

## 8. テスト方針

`tests/test_rotation_service.py` で合成 DataFrame を入力し、各計算関数を検証:

- セクターリターンの等加重平均が正しいか（分割日を跨いでもスケール破綻しないか）
- turnoverRatio が「当日 ÷ 中央値」で正しく出るか
- 鮮度の3分類が weekRank/monthRank の境界で正しく振り分くか
- 資金流入5成分の合成が 0-100 に収まるか
- 構成銘柄3未満の業種が除外されるか
- 52週シグナルが `stock_cache` の `sepa_from_low/high`・`mom_new_high` から引かれ、
  prices からの260日再計算をしていないか
- margin アーカイブ不在時に信用指標がスキップされ段階1が動くか（グレースフルデグレード）
- （段階2）JPX PDF パーサ: 保存済みサンプル PDF を入力に、既知の数銘柄（例: 13010 極洋の
  売残2,800・買残162,700）が正しく抽出されるか。フォーマット変更でヘッダが崩れた場合に
  黙って通らずエラーになるか

---

## 9. 非目標（YAGNI）

- リアルタイム板・VWAP・分足（段階3・kabuステーション）
- 細かいテーマ・マスタの構築（S33 業種で開始、将来差し替え可能に留める）
- 事前計算バッチ化（都度計算で実用速度のため。将来重ければ同じ純粋関数をバッチから呼ぶ）
- 米国株（KabuTrend の /us/trend 相当）

---

## 10. Codex レビュー対応履歴（2026-07-26）

| # | 指摘 | 対応 |
|---|---|---|
| P1 | JPX信用残の形式・URL・保持週数が未確定 | **実ファイルをスパイク取得**。PDF形式・URL規則・直近5週保持・列定義・curl取得可を §5.1 に固定。週次アーカイブ方針を明記 |
| P1 | KabuTrend の内部閾値を参考にするのは危うい | §1「設計スタンス」新設 + §4B で **自前分位点・バックテストで決める**方針に変更。写経しない |
| P1 | 詳細ページ遷移がコードと不整合（query param非対応） | **実コード検証**（`selected_code` を session_state で読む）。§E を `session_state`+`switch_page` ボタン方式に修正 |
| P2 | 90日窓と52週高値/安値が矛盾 | §3.2 で **stock_cache の `sepa_from_low/high`・`mom_new_high` 再利用**に確定（260日再読なし）。`sepa_from_low` が実質52週安値距離 |
| P2 | セクター売買代金「合計」は大型業種に寄る | §3.3「加重方針」新設。全成分を**業種内相対値**に正規化しランキングを規模中立に |
| P2 | `flowDominance` の命名が強すぎる | 内部名・UI とも **`上昇売買代金シェア`(`up_turnover_share`)** に変更（§4B.1） |
| 所感 | 「中身を読めたので再現」に寄りすぎ | §1 で **「参考UI + 独自の指標・閾値・検証」**のスタンスに全体を再フレーム |
